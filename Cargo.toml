[package]
name = "discord-bot-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
twilight-gateway = { version = "0.17", default-features = false, features = ["rustls-ring"] }
twilight-http = { version = "0.17", default-features = false, features = ["rustls-ring"] }
twilight-model = "0.17"
anyhow = "1"